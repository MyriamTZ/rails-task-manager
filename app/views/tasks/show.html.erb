<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Back to list", tasks_path%>

<%= link_to "Edit this task", edit_tasks_path%>

<%= link_to "Delete this task",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
